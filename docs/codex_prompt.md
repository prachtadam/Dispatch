Build a working web application based on the master_spec.md.

Requirements:
- Use React for frontend
- Use Supabase for backend (database, auth, storage)
- Create a basic UI with:
  - job list
  - customer list
  - field list
- Jobs must link to customers and fields
- Include lat/lon on fields
- Show a simple map with job pins
- Build a basic dispatch engine function (stub is fine)
- Keep code clean and modular

Important:
- Do not hardcode business values
- Use placeholder/mock data if needed
- Focus on getting a working app running first

Output:
- Full project structure
- All necessary files
- Instructions to run locally
